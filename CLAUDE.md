# tunaRound - Claude Code Handoff

> 이 파일은 다음 세션이 이어가기 위한 핸드오프입니다. 제품/설계 전모는 [docs/design/tunaRound-v1-design_2026-06-29.md](docs/design/tunaRound-v1-design_2026-06-29.md)(현행 spec).

## 표기 / 작업 규칙 (tuna 생태계 공통)

- 사용자 응답·문서는 **한국어 존댓말**. **em-dash 사용금지**(일반 대시 `-` 또는 콜론 `:`). ANSI 박스 드로잉 자제.
- 도메인 도착 URL/도메인은 비노출(소스공개, 서비스 비공개).
- 구현 위임 우선순위(2026-07-06 개정): **① tunaLlama(kimi-k2.7-code:cloud) → ② A2A codex 감독 → ③ Sonnet 서브에이전트.** 아키텍트·스펙·리뷰·검증은 **Opus**. (상위 순위가 스코프/컨텍스트 한계로 막히면 다음 순위로 폴백.)
- 한 세션 한 목적. 검증(build/test)과 commit/push는 분리.

## 맥↔윈도우 협업 규약 (git 교통정리, 2026-07-03)

> 충돌 발원지 = CLAUDE.md "현재 상태" 서술 + 진입점 포인터를 양쪽이 같은 줄로 다시 써서 부딪힘. 규약으로 같은 줄 경합을 없앤다.

- **"현재 상태" 서술 블록 = Windows(home) 단독 편집.** 맥은 서술을 건드리지 않는다. 맥 상태는 맥 자기 핸드오프 파일에 적고, CLAUDE.md 서술 갱신이 필요하면 Windows에 위임한다(또는 A2A task).
- **진입점 포인터 = 줄 분리.** `WIN 최신:` 줄은 윈도우만, `MAC 최신:` 줄은 맥만 편집한다. 서로 다른 줄이라 rebase가 깔끔히 병합된다.
- **세션별 상태·서술 = 각자 자기 핸드오프 파일**(`_session*.md`=윈도우 / `_mac-*.md`=맥). 한 파일을 양쪽이 동시에 열지 않는다.
- **rebase 위생(공통).** 세션 시작 + 매 push 직전 `git pull --rebase origin main`. 작은 커밋 자주 push. 공유 파일 편집을 핸드오프 너머로 들고 있지 않는다.

## 총괄/관리자/실무자 협업 위계 (멀티 에이전트 공유 레포, 2026-07-06)

> 여러 Opus/에이전트가 한 레포에서 동시에 일하며 충돌·중복이 생긴다(실측: 동시 버전 범프 위기, main 직접 편집, 코드 미커밋 방치, 스테일 브랜치). 역할별 권한·격리로 정리한다. **이미 만든 A2A 브로커가 조율 레이어다**(task 배정=선점).

- **총괄** (사람 자리 = win, 통합자): **main 머지 독점.** 방향 결정. **공유 파일 소유**(CLAUDE.md · README · Cargo.toml · CHANGELOG). 관리자/실무자 산출을 PR로 받아 CI green 후 머지한다.
- **관리자** (라이브 TUI 감독, role=supervised): **진단·리뷰·repro·스펙 산출이 주 역할**(2026-07-07 개정). 관리자의 실가치는 병렬 조사·신선한 시각이지 편집이 아니다. **실제 코드 변경은 직접 하지 않고 총괄에 넘기거나 총괄이 dispatch한 실무자 worktree로 집중**시킨다(코드가 여러 머신에 분산되는 것 방지 - mac-claude-sup의 char-패닉 직접 PR 실측 교훈). 예외=서비스 다운 등 긴급 수정(그때도 즉시 상류로 넘김). main 직접 push 금지(자기 핸드오프 포인터 줄 = 위 규약 예외만). 배정된 task 스코프 안에서만 움직인다.
- **실무자** (헤드리스 워커, role=worker): **1 task → 1 브랜치/worktree.** worktree로 물리 격리(병렬 충돌 원천 차단). 무관 파일 손대지 않고, 끝나면 `complete_task`(실패 시 `fail_task`)로 보고한다.

**핵심 규칙 5개**:
1. **사소하지 않은(non-trivial) 변경 = 브랜치.** main 직접 편집 금지, main = 총괄 머지 전용(CI green 전제).
2. **한 브랜치 한 에이전트.** 병렬 워커는 worktree로 격리(같은 브랜치 동시 편집 금지).
3. **작업 선점 = A2A task로.** 총괄이 goal/send_task로 배정 → 에이전트 claim = "누가 뭐 하는지" 직렬화(중복 작업 방지). 큰 작업 전 로스터/tasks로 겹침 확인.
4. **미커밋 들고 있지 않기.** 코드 변경은 곧 브랜치 커밋(작업 공간/실행에 반영되었다고 해서 커밋된 것은 아님). 스테일 브랜치는 정리한다.
5. **동기화 위생**(위 git 교통정리와 동일): 세션 시작·push 전 `pull --rebase`, 작은 커밋 자주, 공유 파일은 총괄 경유.

> **배정 → 위임 (책임의 이전).** 위 위계가 "누가 뭘 하냐"라면, 위임 행동 규약은 "총괄이 손을 떼도 되냐"다. 관리자·실무자는 루틴 문제를 자가 처리·재시도하고 terminal(completed/failed)로 반드시 보고하며, 막힐 때만 (무엇이/왜 막혔는지/뭐가 필요한지)를 명확히 에스컬레이션한다. 총괄은 매 task를 폴링하지 않고 `watch-results` 인박스로 결과만 받는다. 규약=[a2a-usage §7](docs/reference/a2a-usage.md)(구 §12·§13은 2026-07-12 재작성에서 §7 축약본으로 대체, 프리앰블 원문은 git 2e2ce60에만).

## 개발 행동 규율 (이 프로젝트 실험 적용, 2026-06-29)

> 전역 규칙 아님. 이 레포 실험 적용. **전문·근거·예시·위임 라우팅은 [docs/reference/development-guidelines.md](docs/reference/development-guidelines.md)**.
> 10개 중 #1·#2·#3·#4·#8·#9·#10은 전역 COMMON.md가 이미 always-on으로 강제하므로 여기 중복하지 않는다. 아래는 이 프로젝트 신규 3개만 둔다.

- **#5 한국어 문장 끝은 마침표.** 리스트/예시 앞이라도 `:`로 끝내지 않는다. 콜론은 라벨·key-value·문장 중간만.
- **#6 새 소스 파일 첫 줄 = 역할 한국어 한 줄 주석.** Rust 예: `// 토론 라운드 프롬프트를 조립하는 순수 함수`. config 파일 제외.
- **#7 비trivial 작업 전 plan + `checklist.md` + `context-notes.md`.** plan만 주고 코딩 요청 시 멈추고 checklist·notes 먼저 만들지 묻는다.

## 현재 상태 (2026-07-19, 세션 6~32. 세션32 = 백로그 완주: #138·#115·#147 종결 + v0.6.0 릴리스·배포)

- **세션 32 (2026-07-19): #138 완주 → v0.6.0 릴리스·양 머신 배포 → "모두 진행" 전 백로그 스윕 → Kiwi 백엔드 정리.** ① **#138 A·B 완주**: A-1(#139 get_task 클라 타임아웃 계약+`task get --wait`)·A-2(#140 stdin 실패 가시화) + B 4모듈 순수 이동 분해(#141 server.rs 3,121→291 / #143 repl 2,128→19 / #144 presence_scan 1,920→496 / #145 worker 1,657→41, 전부 정규화 diff 바이트 등가+적대 검증 PASS). ② **v0.6.0 릴리스**(태그 bd93395, 전 잡 success)·win rename-swap·mac 아티팩트 정렬. ③ **스윕**: 실주제 토론 2건(debate:39a438949e9c=#147 좌석 수신함 만장 합의 / debate:036baa9d5f92=C-④ b 기각 증명→c즉시+a예약, **각 이슈 코멘트=계약 정본**, ④게이트 2/2 겸용) + PR #142·#148~#157(rusqlite 0.40=코드 0·tungstenite 0.30=1줄·**rmcp 2.2**=스냅샷 오라클 deep-equal 0 diff+구클라 왕복 하위호환 실측 / 좌석 수신함 Stage 1 / 경계 테스트 2갭 / 다크 스크롤바) → **#115·#138·#147 CLOSED**, #151 신설. ④ win mesh 재배포(rmcp 2.2 라이브)+훅 3파일(--also-agent 배선), mac은 wire 호환 실측이라 0.6.1 때 정렬(**바이너리→훅 순서 제약**). ⑤ **Kiwi A2A 2건**: #158~#160 머지(win 스크립트 잠복 버그 3건+리뷰 3연쇄 실검증) / **serve 브로커에 kiwi.dll 실매핑 판정**=공유 검색은 Kiwi·전체 종결. ⑥ 사고: rust-analyzer 커밋 65GB 누수→빌드 침묵사·스캐너 파편사→복구(메모리 2건). ⑦ **버전 정책 사용자 교정: 다음=0.6.1 patch**. rustup 1.94→1.97 전역 갱신됨(libsqlite3-sys 0.38 요구). **다음=① v0.6.1 릴리스(도그푸딩 후 승인) ② 오너 결정 2건(#151 KIWI_RS_VERSION 배선) ③ 트리거 대기(C-④a wedge·#147 Stage2·C-①).** main=4e0b946+핸드오프. 진입점 [세션32 핸드오프](docs/prompts/v2-handoff_2026-07-19_session32.md).

- **세션 31 (2026-07-18~19): 승인 게이트(#131) 설계→구현→라이브 검증 20항목 + v2-54 P2 + #115 ①② + 유령·#136·#133 종결 + 리팩토링 트랙(#138) 준비.** ① **게이트 #131=PR #132 머지·CLOSED**: `start_discussion(gate:true)`→라운드마다 다이제스트 인박스→`continue_discussion(id, steer?, conclude?)`. 설계·코드 각 3렌즈 적대 검증(major 3: **대기 표식 task**=재기동 침묵사 봉합 / **승인 주체=사람 문구 계약** / 실패=중단). 라이브 검증=토론 4건(실주제 steer 조향이 산출 실변경 / conclude 직행 / 대기 중 stop / **게이트 대기 중 재기동→표식 failed 인박스 재생 배달 실증**). 실주제 산출=#115 착수 순서 합의(이슈 코멘트=정본: ①②→③→⑤→④, ④=이벤트 게이트+트립와이어). ② **P2=PR #134**: get_task wait_secs 롱폴·node 토큰 config 폴백(dotenv 파서=훅 정본 의미론 정렬)·dispatcher 완전일치 규약. ③ **#115 ①②=PR #135**: toml 1.1.3·lindera 4.0.0 코드 무변경, R@5 재측정 완전 동일·audit 0·NOTICES 재생성. ④ **#133 CLOSED**(PR #132 일부+#137): 실벽시계 gc×고정 리터럴 시한폭탄 9건 해체(적대 스캔 잔여 0). ⑤ **"기타" uuid 유령 근절**(사용자 리포트): mac 구 poll(--no-register 부재) 자가 교체+deregister→스캐너 재소유. **#136 발견→종결**: 원인=프리뷰 절단+수신 오진(head -1·completed 대상, 도구 결백 실측), 처방=훅 수신 절차(claim 전 get_task 전문)+발신 요지 선두 규약 양 머신 배포. ⑥ win mesh 재배포 2회(최종=wait_secs·toml1·lindera4 포함). **다음=① 이슈 #138(v0.6.0 전 리팩토링: A-1 get_task 타임아웃 계약·A-2 exec stdin 가시화=검증 완료 즉시 착수, B server.rs 3,121 분해 - ⚠ #115 ④ 게이트 카운터 리셋 관계) ② v0.6.0 릴리스 ③ #115 ③ rusqlite.** main=97c1062+핸드오프. 진입점 [세션31 핸드오프](docs/prompts/v2-handoff_2026-07-19_session31.md).

- **세션 30 (2026-07-17~18): v2-56 mesh 토론 완주(설계→Phase 0 게이트→구현→라이브 실토론) + 대시보드 피드 md/토론 뱃지/발신 필터 + 이슈 #123 턴 스피너 + 문서 스윕.** ① **v2-56**: 사용자 발의(토론을 멀티세션 구조로)→스케치(75c268f, 병렬 조사 6+적대 검증 3렌즈: Stage1 폐기·Phase0 게이트 신설·synthesizer '개정' 재분류·driver 폴링 단순화)→**Phase 0 PASS**(코드 0 운영 레시피로 크로스머신 2라운드 토론, 주제=재기동 처리 설계 자체 - 토론이 설계를 실개선: 락 자기모순·침묵사 창 발견)→**PR #127**=driver(src/discussion.rs)+start/stop_discussion MCP+기동 고아 sweep, 코드 적대 리뷰 3렌즈 major 전량 반영(**타임아웃·중단=failed 마감**=인박스 통지, canceled는 watch-results 미배달=침묵사)→**라이브 E2E 토론 12049ab3a0e4**(win 워커 proposer+mac 라이브 reviewer→종합, 인박스 RESULT 3건·전사 4행) - **합의 산출="라운드 간 사람 승인 게이트가 다음 1순위"**(이슈화=사용자 결정 대기). 세션12 "크로스머신 토론 비목표" 개정(사용자 발의, §0 명시). ② **대시보드**: PR #128(카드 상세 md 렌더·debate 뱃지·발신 필터)+PR #129(사용자 리포트 2건: 미리보기 md 렌더 2줄 클램프 / **CJK 인접 `**` 미볼드=CommonMark 플랭킹→remark-cjk-friendly**). ③ **#123 CLOSED(PR #130)**: claude=UserPromptSubmit↔Stop 훅(신설 tuna-turn-end.py) turn-ping 쌍 / codex=스캐너 rollout mtime→active_at / busy 합류(신선도 훅 600s·mtime 90s, sync mem max-merge 필수). win 라이브 검증(점등→소등→400)+훅 배포·Stop 등록, mac=A2A task 22df407f 자율 배포(편차=dashboard 피처 제외, mac 미서빙 dead code라 수용). ④ **문서 스윕**: CHANGELOG [Unreleased] 4건·README(토론=mesh 정본·**대시보드 릴리스 포함 스테일 정정**)·스윕 에이전트 발견 7건(onboarding·dev-mac-windows·source-run·config.example Stop훅). **다음=① 도그푸딩 관찰(mac codex 턴 스피너·실주제 토론 1회) ② 게이트(continue_discussion+steer) 이슈화 여부 사용자 결정 ③ v0.6.0 릴리스(Unreleased 4건 적재) ④ 백로그(#115=rmcp·tungstenite가 v2-56 코드층과 겹침 주의 / v2-54 P2).** main=e689a20+핸드오프. 진입점 [세션30 핸드오프](docs/prompts/v2-handoff_2026-07-18_session30.md).

- **세션 29 (2026-07-14~16): v0.5.0 릴리스 발행·양 머신 배포 + 이슈 일소 + dependabot 정비 + 온보딩 P0+P1 + README 재작성.** ① **세션28 잔여 소화**: mac relay 재배포(964cba8e) / **B-2·B-3(PR #113)** = dist features+dashboard·github-build-setup(npm 주입)·NOTICES 생성·include. ② **dependabot 첫 가동 정비**: release.yml=cargo-dist 소유 충돌(#109 close→PR #114 first-party 4종 ignore+수동범프), #110 머지, #111 close→**cargo=lockfile-only(PR #116, Rust 0.x는 semver 분류상 major-ignore 불가)**+메이저 5종 백로그 이슈 #115. ③ **이슈 일소**(정찰→스펙→sonnet 3병렬 worktree→중앙검증→PR→봇 전수→머지): #94 스피너(PR #120, 서버 fresh-busy+SSE transient, 라이브 검증) / #118 유령 poll(PR #121, --session-marker 자가종료, 양 머신 스모크 PASS, 구세대 잔재 win9+mac1 소진) / #119 codex 래퍼 마커(PR #122, hybrid 게이트. **loaded/list는 사용자 협업 프로브로 기각** - TUI 닫아도 잔류 실측. win=PATH 잔존 즉시 활성, mac=~/.local/bin 배치를 **codex 조사→claude 처리 다중 홉 체인**으로 수행=relay E2E 재검증 겸). ④ **온보딩**: v2-54 감사(5영역 병렬, docs/design/v2-54-...md) → P0+P1(PR #124: init 로컬 프리셋·MCP 자동 등록(get→add 멱등, bounded 데드라인)·러너 전수 lane·node 사전검증, 샌드박스 라이브 검증) → **README 재작성(PR #125**, 지도=docs/prompts/readme-rewrite-guide_2026-07-15.md, 첫 화면=init·node·재시작만) + onboarding §2 로컬 첫 왕복. ⑤ **v0.5.0 릴리스**: 프렙 #126(CHANGELOG 큐레이션 45PR·NOTICES 재생성) → cargo release minor → run 전 잡 success(**B-2 build-setup 4러너 실전 검증, zip에 NOTICES+SPA 임베드 확인**) → 양 머신 릴리스 아티팩트로 배포. ⑥ 이슈 신설 #123(세션 동작 표시 백로그). **다음=재부팅 복구 확인(2026-07-16 재부팅, restart 스크립트 실행해둠) → 자연 도그푸딩 관찰(#119 래퍼·#94 스피너) → 백로그(#123·#115·v2-54 P2).** main=릴리스 후 문서 커밋들. 진입점 [세션29 핸드오프](docs/prompts/v2-handoff_2026-07-16_session29.md).

- **세션 28 (2026-07-13~14): fable 5 전체 코드베이스 리뷰(103 findings) → 약 85개를 15개 PR로 봉합·전량 머지 + CI 공급망 하드닝 + main 브랜치 보호 룰셋 + codex-relay 비동기 재설계(라이브 검증·win 배포).** ① **리뷰**: 멀티에이전트 워크플로(서브시스템 13+크로스컷 6 파인더 → 적대 검증)로 103 confirmed(major 29·minor 74) 도출, 전문=스크래치패드 `tunaround-review-2026-07-13.md`. ② **수정 PR 15개**(#95 fail-visible·#96 보안·#97 이식성·#98 워커·#99 codex·#100 검색·#101 REPL·#102 프론트·#103 watch/mcp·#104 store무결성·#105 mesh운영·#106 테스트커버리지+goal loopback#29·#107 정리·#108 CI하드닝·#112 relay): 매 PR=sonnet 실무자 패치→Opus 중앙검증(fmt·clippy all-targets 풀피처·test)→CI 3-OS green→CodeRabbit·gemini 봇리뷰 전수반영→머지. 테스트 597→683(+86). ③ **CI 하드닝(#108)**: permissions read·persist-credentials false·서드파티 액션 SHA 핀·dependabot(groups)·release-features/scripts/audit 잡 신설. ④ **브랜치 보호**: main-protection 룰셋(id 18893642, PR필수+6 CI체크 required+non-fast-forward+admin bypass:always). teeth 확인(#112 windows CI 끝까지 BLOCKED). ⑤ **relay 라이브 검증(#112)**: 주입 중 heartbeat/lease(#8·#62)·ws 도달성 게이트(#65)·취소불가 명시(#9). win-codex-home 라이브로 검증 후 win mesh 배포(mesh.pids 39244/18520/44688/38324, 2+2=4·7×6=42 확인). ⑥ **교훈**: GitHub Actions 일시장애=rerun으로 복구(코드 아님)·중앙검증에 fmt 필수·relay는 broker 무중단 relay만 교체로 검증·app-server 취소 API 없음(#9 한계)·is_multiple_of/ws_reachable-cfg gemini HIGH는 오판. **다음=① mac relay 재배포(win만 배포) → ② v0.5.0 준비 때 대시보드 릴리스포함·라이선스 NOTICE.** main=`4d08d68`. 진입점 [세션28 핸드오프](docs/prompts/v2-handoff_2026-07-14_session28.md).

- **세션 27 (2026-07-13): 이슈 #88 시간창 게이트 정본화(PR #92 머지→#88 CLOSED) + 대시보드 동작 스피너·러너 아이콘(PR #93 머지·WMI 재배포 v0.4.0 라이브) + 티키타카 테스트 + 스피너 버그 #94 발견.** ① **PR #92 = #88**: 라이브 실측(codex-cli 0.144.1)으로 대안 기각 - #1(session_meta PID 부재)·#2(app-server `thread/list` status·`loaded/list`가 인스턴스별 + 죽은 thread도 `thread/resume` 성공→idle/loaded 오염=악화, 사람 TUI는 VS Code 자체 app-server라 도달 불가). **깨끗한 per-thread 생존 신호 없음** → **사람활동 시간창 게이트 정본화**(`apply_codex_human_input_gate`, human_input/created_at≥now-window면 유지). **적대 검증 3렌즈 GO**("#2 viable" 반증 실패로 정본 정당성 확증), 수용 잔여=방금 닫은 세션 window 잔존(테스트 명시) + minor2 강화(256KB tail 밖 human_input 캐시 유지). lib 597 pass. ② **PR #93 = 대시보드**: 동작 스피너(`busy`=working task의 to_agent, accent 링) + 러너 아이콘(RunnerIcon 복원). 봇 리뷰 3건 반영(스피너 a11y role·dead 클래스). DeepSource JS red=out-of-diff 자문성([[deepsource-python-fails-on-main]]). ③ **티키타카**: mac-claude "2+2=4"·win-codex "3+4=7, DESKTOP-BB1EP5U의 codex" 실제 왕복. ④ **배포**: WMI 스폰 재배포 v0.4.0 라이브(broker PID 12320, 9/9 online). ⑤ **⚠ 스피너 버그 발견 → 이슈 #94**: `state=working`이 "지금 활동 중"의 나쁜 프록시라 stuck task 영구 FP(`t-978e` mac-codex 7분+ working)+빠른 task 5초 폴 놓침 FN. 수정=SSE 실시간+stale 타임아웃(다음 세션). **다음=① fable 5로 프로젝트 리뷰 먼저 → ② 스피너 버그 #94 패치.** main=`84efe21`. 진입점 [세션27 핸드오프](docs/prompts/v2-handoff_2026-07-13_session27.md).

- **세션 26 (2026-07-12): 잠복 이슈 3건(PR #89) + v2-52 리팩토링 전체 완주(⑤ store DTO=#90·④ task JSON Stage1=#91) + 배포·도그푸딩 + 이슈 #88 착수(부분→전체 #2 후속).** ① **PR #89**=post_turn R1 계약(success→error)·index race 직렬화(데드락 없음)·embed 타임아웃. ② **PR #90 v2-52 ⑤**=`ConversationSnapshot` 중립 타입(serde 없음)으로 SQLite 스키마·트리 상태머신 누수 캡슐화(store From 격리, SQLite 오라클 불변), S0~S6. ③ **PR #91 v2-52 ④**=poll_tasks 프로토콜 JSON 병존(`TASKS_JSON` 프리픽스 + human 블록, 워커 JSON 우선·문자열 폴백, 라이브 mesh 4조합 하위호환, 무게이트 `a2a_wire`). Stage 4(문자열파서 제거)만 post-rollout defer. ④ 각 PR **적대 검증 워크플로우(전부 GO, blocker/major 0)** + 봇 리뷰 전수 처리. **v2-52 리팩토링 백로그 6항목 전부 완료.** ⑤ **배포**: WMI 스폰 `restart-win-mesh.ps1 -SourceBin`(새 바이너리), **④ JSON 프로토콜 라이브 검증 성공**(poll에 `TASKS_JSON [{...}]` + human 블록 병존). ⑥ **이슈 #88**(codex presence 유령 오라우팅) 착수: 시간창 게이트 구현했으나 적대 검증=**부분 완화(완전 해결 아님)** → 사용자 결정 **전체 해결(#2 app-server thread/loaded/list)을 다음 세션**(브랜치 fix/issue-88@`592121e`). ⑦ **GPT 온보딩 답변 검토**: 대시보드-릴리스-포함만 유효(그 외 페어링/위저드=competitive lens 비채택). **다음=① 이슈 #88 전체 해결(#2) ② v0.5.0 릴리즈(도그푸딩·승인, 대시보드-릴리스-포함 P0).** main=`1ec59b3`. 진입점 [세션26 핸드오프](docs/prompts/v2-handoff_2026-07-12_session26.md).

- **세션 25 (2026-07-12): v2-52 리팩토링 백로그 clean 스윕 완주 (PR #83~#87 5개 머지, 전부 순수 코드 이동=동작 불변·적대 diff 리뷰 등가·CI green·봇 반영).** ① **PR #83**=main.rs 분리(서브커맨드 dispatch·백엔드 빌더→`src/cli_run.rs`, 1,160→618). ② **PR #84**=토크나이저·임베더 팩토리 dedup(#83 봇 제안). ③ **PR #85**=전역 `cargo fmt --all`(49파일) + CI 신규 `fmt --check` 잡 + clippy `--all-targets` 확대. ④ **PR #86**=mcp.rs 분리(17 rmcp #[tool]→named tool_router 합성으로 mcp/{search,tasks,registry}.rs+indexing.rs, 1,660→983). ⑤ **PR #87**=store/sqlite/tasks.rs 분리(21 impl 메서드→tasks/{state,lease,replay}.rs, 1,880→1,341). ⑥ 스테일 브랜치 전량 정리(origin=main만). ⑦ **rmcp 1.8 named tool_router 합성 확정**(서브모듈=자식이라 private 접근, 위임 불요). **④ task JSON·⑤ store DTO는 defer**(사용자 결정: 프로토콜/아키텍처 변경이라 전용 세션·계약 고정 먼저). **발견 잠복 이슈 3건**(post_turn error 계약·index race·embed timeout, pre-existing) 별도 처리 대상. **다음 = v0.5.0 릴리즈(도그푸딩 후) + v2-52 잔여(④⑤)·잠복 3건.** main=`f795ba3`. 진입점 [세션25 핸드오프](docs/prompts/v2-handoff_2026-07-12_session25.md).

- **세션 24 (2026-07-12): v2-48 재대조(defer 유지) → presence 타임라인·큐레이션 기억 병렬 구현 → 품질게이트 → 대시보드 전면 재편 → 도그푸딩 배포·라이브 검증 (PR #76~#82 7개 머지).** ① **PR #76**=v2-48 감독 레인 3축 재대조→still_churning, defer 유지(트립와이어 §0 기록: #34922 GA·07-10/11 가시성 클러스터·캐던스 둔화). ② **PR #77 v2-50**=presence 타임라인(스키마 **v11** `presence_events` append-only + edge 로깅 + `/dashboard/presence-timeline` + 패널). ③ **PR #78 v2-51**=큐레이션 기억(`/annotate` + abstraction/anchors 활성화, 스키마 불변). ④ **PR #79**=CHANGELOG [Unreleased]. ⑤ **PR #80 v2-52**=리팩토링 백로그 문서(Codex 전수조사). ⑥ **PR #81**=품질게이트(clippy `--all-targets` 3건·exec.rs Windows 이식성). ⑦ **PR #82 v2-53**=대시보드 전면 재편(3층 IArch: 헤더 액션 / 사이드바 로스터·본문 관제 / 푸터 상태. lucide-react·테마토글·서버소스 요약타일·전폭 피드+드롭다운 필터 50→200·presence 로그·브레싱+하트비트 닷). ⑧ **도그푸딩 배포**: broker.db **v10→v11 마이그레이션** 라이브, WMI 스폰, Chrome 라이트/다크 검증. ⑨ **생존확인 5/5**(무응답 0)·**v0.4.0 릴리즈 확인**(brew·아티팩트 발행). ⑩ **메모리 정정**: 사용자 호칭(사장님·악기 disavow)·north-star 실제 방향(팔지않되 OSS 공개). **다음 = v0.5.0 릴리즈(도그푸딩 후) + v2-52 리팩토링 백로그.** main=`d6774ac`. 진입점 [세션24 핸드오프](docs/prompts/v2-handoff_2026-07-12_session24.md).

- **세션 23 (2026-07-12): v2-47 마무리 + 문서 리프레시 + Codex 제안 검토 채택 + 설치 단순화 + v0.4.0 릴리즈 (PR #70~#75 6개 머지·배포·맥 알림).** ① **PR #70**=v2-47 #3 후속(브로커 헬스에 uptime·WAL: store db_path+get/set_config+wal_bytes, config 테이블 재사용=마이그레이션 불요, fail-visible). ② **PR #71**=v2-48 워커 러너 현황 정정(이미 완성·opencode v1.17.18·fixture 테스트, 감독 레인 R2 defer). ③ **PR #72**=v2-47 낮은우선(★ recency·관전 뱃지·모바일 반응형). ④ **PR #73**=README 650→171줄 리프레시(개인악기 톤) + 신설 onboarding.md·mesh-architecture.md. ⑤ **PR #74**=Codex 제안 채택분: task lease 자동연장(#6, 장기 task requeue 잠복버그 수정)+cancel_task MCP/CLI(#4). 나머지 8제안=SSE·watch-results·lease 재발명이라 비채택. ⑥ **PR #75**=init 원커맨드 온보딩(node.toml+~/.tunaround/config 동시 스캐폴드)+토큰 env 통일(TUNA_BROKER_TOKEN)+AI 설치 안내(docs/prompts/install-with-ai.md). ⑦ **브랜치 정리**: 머지분 전량 삭제·session16 문서 편입(`2e65e12`), 미머지 session17만 잔여(삭제 권고). ⑧ **v0.4.0 릴리즈**: cargo release minor→태그 push→cargo-dist(run 29171411699). ⑨ **배포**: WMI 스폰 0.4.0 라이브·mesh 무결. **다음 = v2-48 opencode 감독 레인(스키마 재대조 후)**. main=`6c78dbe`(v0.4.0). 진입점 [세션23 핸드오프](docs/prompts/v2-handoff_2026-07-12_session23.md).

- **세션 22 (2026-07-12): v2-47 대시보드 관제탑 고도화 5개 주 항목 완주(PR #67·#68·#69 머지·배포·라이브 검증).** 세 소 PR로 각각 `구현 → 적대적 리뷰 → CI(clippy 3-OS·dashboard SPA·CodeRabbit) → 머지 → WMI 스폰 배포 → Chrome 라이브 검증` 완주. ① **PR #67 = #1·#2**(피드 카드 상세 펼침: 요청·결과·실패 사유 / 필터 칩: 상태·머신·러너·텍스트). ② **PR #68 = #3·#4**(브로커 헬스 패널 `GET /dashboard/health`: 열린task·미배달·고착 집계+머신별 스캐너 도달성, `classify_task_health` 단일 소스 / 브라우저 알림 옵트인: 완료·실패 SSE, 재생·재접속 중복 발화 방지). ③ **PR #69 = #5**(위임 이력 검색 `GET /dashboard/search?q=`: MCP search_context와 같은 retriever 재사용=형태소+FTS, 별도 retriever-state 서브라우터 merge, **결과를 `a2a/*` 화자로 스코프**=비-a2a 전사 무인증 노출 차단, 배포 바이너리는 semantic 미포함이라 embedder 네트워크 비의존). ④ **적대적 리뷰 실이슈 3건**: index-as-key→안정 키 / 헬스 spawn_blocking 실패=전부0 위장→500 표면화(CodeRabbit) / 검색 surface=전체 FTS→a2a 스코프. ⑤ **결정**: #3 uptime·WAL은 store 표면 변경 필요라 후속 / #5는 탭 대신 자체 섹션 / 관제탑 원칙 불변(read-only 강화). ⑥ **교훈**: DeepSource JS/Rust=자문성(main 미보호, canonical=clippy·dashboard·CodeRabbit), idiom 신규 라인 재귀속은 머지 후 사라짐(메모리 기록). **다음 = 방향 선택**(v2-47 낮은우선 2건 / #3 후속 uptime·WAL / v2-48 opencode / 스테일 브랜치 정리). main=`9f2e238`. 진입점 [세션22 핸드오프](docs/prompts/v2-handoff_2026-07-12_session22.md).

- **세션 21 (2026-07-11): v2-45 아크 설계 확정 + P0·P1·P2 머지·배포·라이브 검증 + 대시보드=관제탑 확정 + /handoff 전역 스킬.** ① **설계 정본 [v2-45](docs/design/v2-45-mesh-persistence-and-replay_2026-07-11.md)**(병렬 조사 7 에이전트 + gap-check → §5 고정 계약 9건 = 구현 스펙 정본, 매 P 착수 전 재독). ② **PR #57 = P0**(직접 제어 /dashboard/control+ControlForm 전체 제거 -305줄) / **PR #58 = P1**(watch-results 재접속 루프+SeenSet 상한+수립 시점 생존 판정 - 배포 순간 구판 즉사로 결함 라이브 재현 실증) / **PR #59 = P2**(list_tasks_replay 공용 질의+envelope 헬퍼 통일+`?replay=50` 선행 프레임+`?since` 표면 - **리로드해도 피드 유지 라이브 검증**). ③ **사용자 결정**: 대시보드=관제탑 충실(제어 UX 비확장·웹 human 신호 비채택) / 세션 분할 P3까지→P4·P5→P6a·b→P7·P8 / P8 승격(하트비트 비채택). ④ **사고 규명**: mesh 전멸 진범=구현 에이전트 taskkill //IM(이름 전수 종료 금지, 스모크는 PID로만 - 메모리 기록) + 배포 재기동=WMI 스폰 확립. ⑤ **백로그 문서**: [v2-47 대시보드 고도화](docs/design/v2-47-dashboard-observatory-backlog_2026-07-11.md)·[v2-48 opencode 배선](docs/design/v2-48-opencode-wiring_2026-07-11.md)(정찰 확정, 포트=설정 고정 가능, 착수=스키마 냉각 후). ⑥ **/handoff 전역 스킬**(~/.claude/commands, win-codex-home A2A 위임 생성) + 어텐션 핑 5/5(감독 전원 왕복). **다음 = P3 마무리(핸드오프 §4 분기) → P4·P5. ⚠ 다음 세션 모델=Opus 4.8.** 진입점 [세션21 핸드오프](docs/prompts/v2-handoff_2026-07-11_session21.md).

- **세션 20 (2026-07-11): 재부팅 복구 스크립트 + v2-46 codex-relay(sup 폐기) 양 머신 완주 + T5 정리 + 운영 견고화 A.** ① **PR #53**: restart-win-mesh.ps1(재부팅 복구 1줄) + presence 스냅샷 10초 데드라인(이 머신 tasklist 무한 행 실측 = 로스터 win 전멸의 근본 원인) + 훅 Monitor 경로 정규화(exit 127). ② **PR #54 = v2-46**: codex-relay 데몬(로컬 codex 세션 대리 claim → 보이는 세션 thread 직주입, 실패=fail_task) + codex-inject --thread + GoalForm codex 카드 복귀 + **유령 B석 tombstone**(.ctx 삭제→"dead" 기록, 삭제=보수적 유지의 자기모순 해소). E2E win 403·mac 실증, **mac 이관 = A2A task 자율 수행**. 세션19 §4 win attach 타임아웃 = 해소 관찰. ③ **PR #55 = T5**: supervised alias 제거·candidates/discover 전삭제(스캐너가 대체)·a2a-usage §10 재작성+infra 태그 규약 명문화. ④ **PR #56(머지) = 운영 견고화 A**: mesh.pids 선별 종료+rename-swap(**재기동해도 세션 수신 poll 생존** 실측 - luckyCAD exit 127 근본 해소) + task get 열린 task 본문 + 피드 uuid→이름. ⑤ **조사 확정 2건(B 입력)**: 피드 리로드 전멸=이벤트 백필 없음 / ★ 결함=human_input_at 인메모리(재기동마다 증발)+codex 입력 무신호. ⑥ 부수: statusline 복원(시스템 python), tunaCTX 삭제=의도(메모리 기록). **다음 = B(v2-45: watch-results 재접속·재생, 피드 스냅샷, ★ 영속·codex 신호, mesh 기억화, Redis opt-out) → C.** 진입점 [세션20 핸드오프](docs/prompts/v2-handoff_2026-07-11_session20.md).

- **세션 19 (2026-07-11): v2-44 완주(PR #47~#51) + 유령·깜빡임 근절 + 수신 완전 자동화 + codex "보이는 세션" 전환 + 거버넌스 개정.** ① **v2-44 전 구간 라이브**: presence 스캐너(머신당 1, 일괄 diff)·supervised→infra alias·`tunaround task` CLI·watch-results --digest·훅 다이어트·T4 인프라 도트·T4.5 main.rs 분할(2,552→1,155줄). ② **유령·깜빡임 근본 3원인 파쇄**: 세션 owner-pid 마커(.ctx, /clear·창닫기 즉시 제거) / tasklist 부하 에러 가드(실측 5회 중 3회 exit0 에러출력) / 수신 poll `--no-register`(무태그 재등록의 '기타' 유령). ③ **수신 자동화**: 새 세션=SessionStart 지시문, 기존 세션=ping .rx 1회 주입 → 세션이 스스로 Monitor(poll) 가동(사용자 A2A 언급 불요, 세션당 1회 ~300토큰·상시 0토큰). ④ **codex 주입=보이는 세션 thread**(글루 폐지, mac 실증·win은 멀티클라이언트 attach 타임아웃 미해결→T4.7). ⑤ **정본 재확정(사용자)**: TUI 세션=전부 감독세션 상호 송수신 / sup·헤드리스=피드 외 비노출 / **감독발 task 승인 게이트 폐지**(메모리+맥 전파) / Redis 서서히 폐기. ⑥ A2A 왕복 매트릭스 실측(mac-claude 완전자동·mac-codex·win-codex) + DB 정리(WAL 체크포인트·스테일 task). **다음 = PR #52 머지 확인 → T4.7(codex-relay: sup 정체성 폐기·세션 thread 주입·GoalForm codex 복귀).** 진입점 [세션19 핸드오프](docs/prompts/v2-handoff_2026-07-11_session19.md).

- **세션 18 (2026-07-10~11): PR #36~45 열 개 머지 + #46(로스터 리디자인) 열림. v2-43 배선 4/4 + 재검증 매트릭스 전 구간 실측.** ① **배선 완결**: deregister·claude pull 수신·codex 래퍼(PATH shim)·워커 섹션(#36~38). ② **codex "회귀" 오판 정정**(#39, 소스 대조): plain `--remote`는 원래 새 thread - **관전 = `codex resume <threadId> --remote ws://...`**(win 실측 완료). ③ **로스터 오염 3층 근절**: 유령 poll 부활(#40) / 무장 경합+temp 노이즈(#42) / 자동 이벤트 wake의 ★ 탈취(#44, UserPromptSubmit이 Monitor wake에도 발화 → `<task-notification>` prefix 필터). ④ **크로스머신 총감독**(#41): human-ping·deregister 원격+Bearer - 맥 입력 → ★ 이동 실측("사람이 앉은 자리"의 머신 독립성). ⑤ **매트릭스**: codex 관전·claude pull·워커·codex 풀루프+watch-results 깨움·mac 크로스머신(훅 배포 자율 수행) 전부 통과. ⑥ **운영 발견**: 런타임 바이너리 안정 경로 분리(`%LOCALAPPDATA%\tunaround\bin`, 재빌드≠mesh 중단) / **rust-embed debug=dist 런타임 로드**(프론트 반영=npm build만) / MCP native는 세션 시작 시 브로커 생존 필요 / 기동 순서=브로커→app-server. ⑦ **거버넌스 실증**: 맥이 민감 task 운영자-미승인 fail 보고(무해 6건은 자율) + **총괄발 task 자율 규약**(메타 확인 금지) 양 머신 메모리 저장. **v2-44 승인됨(2026-07-11)+확장: sup=머신 인프라 인디케이터 재정의·role 개편(supervised→infra), 정본 [v2-44](docs/design/v2-44-presence-scanner-and-roles_2026-07-11.md). T1~T4 완료(PR #47·#48, T2 ops·T3 mac 라이브, 인프라 도트·스캐너 자기등록·재접속·타임아웃). Redis 서서히 폐기 확정 + 백로그(mesh 기억화·watch-results 재생). + 유령 근절·수신 자동 가동(PR #49). 다음 = T4.5 main.rs 분할 → T5.** 진입점 [세션18 핸드오프](docs/prompts/v2-handoff_2026-07-11_session18.md).

- **세션 17 (2026-07-07~08): 대시보드 라이브 도그푸딩 2아크 → heartbeat=presence 재설계 + 정본 타겟 모델 확정.** (A) **PR #32 머지**: 활동 기반 로스터(v2-41) + codex 관전 결정(§10: --remote 유지·대시보드=통합로그·스트림=헤드리스) + 주입텍스트 절차/답변 분리 + 네이밍 `{machine}-{runner}-{project}` + 뱃지 값만 + discover stale 240 + 봇리뷰 8건. (B) **PR #33(열림)=v2-42/43 재설계**: v2-41 활동모델 결함 실측(총감독이 resume한 세션으로 튐·유령·resume미무장) → **총감독=human_input_at 최신 online 세션**(UserPromptSubmit 훅 핑) + **로스터=online(heartbeat) 세션 전부**(발견/유휴·discover·활동모델 제거). **정본 타겟 모델 [v2-43](docs/design/v2-43-target-model_2026-07-08.md)**: 사람=총괄 1자리 던짐 → 감독/워커 Monitor(poll) 자율수신 → complete → watch-results로 총괄 깨움 → 브리핑. **A2A 워크플로우는 이미 완성**(재발명 금지), 대시보드=뷰. + mac autoarm 전면화 협업. **다음=배선(env→설정파일·codex arming·수신안내·워커섹션), 새 설계 아님.** 진입점 [세션17 핸드오프](docs/prompts/v2-handoff_2026-07-08_session17.md).

- **세션 16 (2026-07-07): 리팩토링 대청소(god파일 sqlite/mcp 분할, 11 PR) + 봇리뷰 전수반영 + SSRF보안 + mac 거버넌스 채택.** main=fca18fb. (핸드오프 docs/prompts/v2-handoff_2026-07-07_session16.md, 별 브랜치 미머지.)

- **세션 15 (2026-07-06): v2-40 유니버설 세션 버스 완성 → 0.3.0 릴리스 → 토큰 로테이션(env 기반) → 거버넌스 규약.** ① 세션14 잔여 PR #11·#12 머지. ② **v2-40 완성**(PR #13): S1 자동무장 훅 · S2 발견 리포터(discover + candidate + automation 노이즈 필터: claude-mem·secall) · S3 후보 패널+연결 팝업 · S4 codex 직접 제어(`/dashboard/control`) · 피드 카드화 · 명칭 **총괄/관리자/실무자**. 보안(CSRF/SSRF/토큰 env 폴백). ③ **토큰 유출 대응**: git 히스토리 퍼지 → **로테이션(새 토큰=User env, argv 노출 0)** → 양 머신 env 기반 재기동. ④ **정체 규명**: observer-sessions=claude-mem(맥), 저널=secall 자동화(윈) → discover 필터. ⑤ **버전 정상화**: 0.2.2 이후 15커밋 방치(exit 2 근본원인) → **0.3.0 bump + v0.3.0 릴리스**(양 머신 설치). ⑥ **거버넌스 규약**(위 "총괄/관리자/실무자 협업 위계" 섹션): 총괄=main 머지+공유파일 소유 / 관리자=브랜치 PR / 실무자=1 task 1 worktree. mac 채택. ⑦ **위임 ≠ 배정 통찰 + 책임의 이전**: A2A 성숙도 논의서 "위임=결과 ownership 이전(총괄이 지켜보는 것에서도 해방)" 구분. **watch-results**(PR #19, 총괄 결과 인박스 = 던진 task 완료를 SSE로 push, 총괄 세션 Monitor로 감싸면 던지고 자리 떠도 깨움) **라이브 실증**(broker.db 폴링=셔틀 대체). **다음=#2 발견세션 패널 복구(mac discover 새토큰 재기동)·위임 행동규약 문서화·role 태그값 명칭통일**(급하지 않음). 진입점 [v2-40-release-governance 핸드오프](docs/prompts/v2-handoff_2026-07-06_v2-40-release-governance.md).

- **세션 14 (2026-07-06): roster 복구 → 총감독 대시보드 완성(목업 React 이식) → v2-40 설계.** ① roster 복구(win watcher `--tags` 재기동 + 맥 자율 재기동 = 3자 감독 online). ② **대시보드 v2-38 T1-T3**(라우트 + 전역 SSE 피드 + roster JSON + goal 폼) → **v2-39 DaleUI SPA(rust-embed 임베드, `dashboard` cargo feature)** → **Claude Design 목업을 plain React로 재이식**(DaleUI 제거, 번들 258→205KB; 통계타일·총감독 표식·heartbeat 애니·shields 값별뱃지·체크박스 멀티선택). ③ **goal 백엔드** `POST /dashboard/goal` = **loopback 무토큰 / 원격 403 read-only 관전**(ConnectInfo). 결정: 로컬=풀컨트롤, 원격=관전. ④ **디자인 반영**(사용자 피드백): 로스터-피드 레이아웃 통일 / 뱃지 값별색 / 아이콘 정돈 / 총감독=대등카드+★토글 지정. ⑤ README 최신화 + **v2-40 유니버설 세션버스 설계**([v2-40](docs/design/v2-40-universal-session-bus_2026-07-06.md): 임의 세션 A2A 주소화·발견·제어, 자동무장 SessionStart 훅) + **Planka 백로그 보드**. **다음=1) PR #12 머지 → 2) v2-40 S1(자동무장 훅).** 브랜치 `feat/orchestrator-dashboard`(bec79fe, PR #12). 진입점 [dashboard-v2-40 핸드오프](docs/prompts/v2-handoff_2026-07-06_dashboard-v2-40.md).

- **세션 13 (2026-07-06): 4자 감독 무-셔틀 mesh 라이브 + 통합 총감독 대시보드 착수.** v2-37 codex 라이브 감독(PR #9=`252e09e`: `codex app-server ws`+`codex-inject` turn/start 주입, 헤드리스 exec 대체)·heartbeat(PR #10=`d7deae3`: poll에 register/heartbeat+`--tags`) 머지. **4자 감독**(win-codex-sup/mac-claude-sup/mac-codex-sup + **win-opus-boss 허브**) 크로스머신 자율 수신·3~4자 티키타카 실증(사람 릴레이 0, 허브도 **Monitor 인박스 자동수신**). 동적 총감독(자리=역할, hydration ritual)·codex narrate 수정(과정 가시)·hydration으로 roster/ack 갭 empirical 발견·정리 실증. **구현 위임 규율 개정**: ①tunaLlama(kimi-k2.7-code:cloud) ②A2A codex ③Sonnet, 아키텍트=Opus. **통합 총감독 대시보드** 설계 정본([v2-orchestrator-dashboard](docs/design/v2-orchestrator-dashboard-and-dynamic-boss_2026-07-06.md))+계획 [v2-38](docs/plans/v2-38-orchestrator-dashboard.md), **T1**(`/dashboard` 라우트+스켈레톤, tunaLlama 생성→Opus 리뷰→적용, 라이브 200) 완료(브랜치 `feat/orchestrator-dashboard`, main rebase). **다음=1) roster 복구(heartbeat로 to_selector 복구) → 2) 대시보드 T2/T3(tunaLlama).** 진입점 [orchestrator-dashboard 핸드오프](docs/prompts/v2-handoff_2026-07-06_orchestrator-dashboard.md).

- **세션 12 (2026-07-04): agentgateway 선별 도입 검토 → D·B·C 3개 PR 머지.** 검토 결론=capability는 태그로 이미 됨 + 값싼 trace/denylist만 취하고 정책엔진·별도 backend registry·lineage DAG는 비채택([검토 노트](docs/design/v2-agentgateway-selective-adoption_2026-07-04.md)). **D**(doctor Stage 4, PR #6=`89cdbf2`): Kiwi/형태소 백엔드 + Ollama 도달 진단. **B**(trace+guard, PR #7=`27f04e6`): tasks `runner` 컬럼(스키마 v8, claim 시 기록) + 쓰기 민감 path 가드(WRITE_GUARD_DIRECTIVE, behavioral). **C**(node태그, PR #8=`5f3ec50`): node.toml lane `tags` 배선(node 워커도 셀렉터 발견) + doctor/node 태그형식 검증. 4개 PR 전부 3-OS CI green + CodeRabbit 반영. 스키마 **v8**(tasks.runner). **위임 vs 회담 정리**: A2A=task 위임(1 task→1 워커, 다중매칭=사람선택), 라이브 다자 토론은 단일머신 `chat --roster`. (⚠ "크로스머신 토론 비목표"는 2026-07-18 사용자 발의로 개정 - mesh 토론 v2-56=`start_discussion`이 정본, docs/design/v2-56-mesh-discussion_2026-07-18.md §0. 팬아웃 프리미티브 없음은 유지.) 태그 셀렉터로 프로젝트별 격리(project= 태그 규율 전제). **다음**: Mac 노드 태그 등록 온보딩(프롬프트 준비됨) / R9 poll 견고화(옵션) / 릴리스 cadence.

- **세션 11 (2026-07-04): 에이전트 레지스트리(UUID 라우팅 + 태그 발견) 구현 완료·머지(PR #5=`2bbf3d3`).** 어드레싱을 자유 문자열에서 UUID(라우팅)+태그(발견)로. 로스터=`SqliteStore.agent_roster`(RefCell 인메모리, `/a2a`·MCP 양 경로 공유). T1 데이터레이어(`src/store/agents.rs` parse_tags/selector_matches/is_online) / T2 MCP 도구 register_agent·heartbeat·list_agents + send_task `to_selector` / T3 `/a2a` SendMessage `toSelector`(공유 헬퍼 store/agents.rs로 이동) / T4 워커 `--tags`·자가 uuid·자동 register/heartbeat(재기동 시 재등록) / T5 문서+라이브 스모크 4/4. 다중매칭=후보반환(사람선택). 하위호환 레거시 `to_agent` 불변. CodeRabbit 4건(R1 에러계약) 반영. 풀피처 414 pass, 3-OS CI green. 정본 [레지스트리 설계](docs/design/v2-agent-registry-uuid-tags_2026-07-04.md) · 계획 [v2-34](docs/plans/v2-34-agent-registry.md) · 사용법 [a2a-usage §9](docs/reference/a2a-usage.md). **부수**: [agentgateway 선별 도입 검토](docs/design/v2-agentgateway-selective-adoption_2026-07-04.md)(결론: capability=태그(됨)+값싼 trace/denylist만 취함, 정책엔진·별도 backend registry·lineage DAG 비채택). **다음 후보(권고 순): D doctor Stage 4 → B(tasks flat trace 컬럼+쓰기 민감 path denylist) → C(config→런타임 태그 seed·backend를 named seat 은닉).**

- **세션 10 (2026-07-04): 브로커 거버넌스 #1~#5 구현 + v0.2.0→0.2.2 릴리스(cargo-dist 6타깃·brew).** 세션9 두 실패(no-consumer·self-disruption)를 구조적으로 제거. #1 네이밍 컨벤션 문서 / #2 빌드 피처 광고(Agent Card `buildFeatures`) / #3+#4 고착·no-consumer 표시 신호 + `tasks` 조망 MCP 도구 / #5 워커 격리 가드레일(`write_lane_disrupts_node`). + `poll --on-task`(0토큰 감독, PR #3) + claim-후-워커사망 자동 requeue(lease 기반, PR #4). PR #1~#4 전부 3-OS CI green 머지. 스키마 **v7**(tasks lease 컬럼). ⚠ Cargo.toml `version="0.2.2"`(더 이상 rc 아님, cargo-dist+brew 정식 배포 중).

- **세션 9 (2026-07-03): R1-R10(PR #1, CI가 R3 Unix버그 포착)·poll 감시자(PR #2·3)·node 고도화 init/node/doctor(PR #3·4·5, 리뷰 8건) + 크로스머신 node 양방향 실증 → 공개 준비.** **filter-repo로 히스토리 272커밋 시크릿 퍼지(사설호스트·SSH·LAN IP·토큰 placeholder화) → 새 PUBLIC `hang-in/tunaRound`(히스토리 보존, 시크릿 0), 옛 것 `-private` rename**(공개 못했던 이슈 해소). README 뱃지·명령 갱신. 발견 2건(win-opus=dispatcher는 폴러 아님 / 워커가 자기 node 클론 write=self-disruption 자살)을 거버넌스로 정리. **다음 세션 = 거버넌스 구현.** 정본 [브로커 거버넌스](docs/design/v2-broker-governance_2026-07-03.md) · [node 온보딩](docs/design/v2-node-onboarding_2026-07-03.md) · [usecase](docs/reference/agent-dev-team.md). ⚠ 레포 PUBLIC이니 문서/코드에 LAN IP·토큰·사설호스트 평문 금지.

- **세션 6: rc.1 CI green 확인 + Windows 아티팩트 검증 + 사설 IP 전방 redact(backend-private.md 패턴) + Stage 3e 킬 -> semi-a2a 파트너 위임(A2A 표준) 설계·Phase 1 코드(Task 1~4 = 데이터레이어·`/a2a` 엔드포인트·worker inbox·dispatcher 툴) 완성·푸시 + Task 5 라이브 크로스머신 도그푸딩 착수.** 검증 기본 209 / 풀피처 262 lib pass. 스키마 **v6**(tasks). 정본 [semi-a2a 파트너 위임](docs/design/v2-a2a-partner-delegation_2026-07-02.md).

- **세션 7 (2026-07-03): Task 5 크로스머신 왕복 성공 = semi-a2a Phase 1 완료.** 윈도우 코어(`serve 0.0.0.0:8770`, LAN 192.0.2.10) `/a2a` SendMessage(win-claude→mac-claude) → 맥 worker poll/claim/complete → 윈도우 GetTask=completed+artifact, 소스 교차검증 통과(task_id 83f0e576, 19:11→19:17 맥 HITL 포함). + **맥↔윈도우 git 교통정리 규약**(단일 통합자 + 진입점 포인터 WIN/MAC 줄 분리, `37a8ee1`).

- **세션 8 (2026-07-03): mac→win 역방향 왕복 성사 = 크로스머신 양방향 다 실증.** 재부팅으로 이전 background 코어(+temp db)가 죽어 옛 task(`907f5c82`) 소멸 -> Windows 코어 재기동(`serve 0.0.0.0:8770`, 안정 db=LOCALAPPDATA) -> 맥이 새 task `76ea0b9c` 재디스패치(같은 주소/토큰, MCP 재등록 불요) -> **win-claude가 raw curl MCP**(claude mcp 등록·세션 재시작 없이 initialize->notifications/initialized->poll_tasks->claim_task->complete_task)로 처리 -> get_task=completed+artifact 자기검증. **교훈 2개**: (a) 워커는 raw HTTP MCP 직접 호출로 "MCP 등록+세션 재시작" 2세션 온보딩 마찰(#1)을 회피 가능(대가=대화형 도구승인 UX 없음). (b) 코어 리셋 시 옛 task_id는 조용히 "task 없음"으로 소멸하고 리셋 신호가 없다 -> dispatcher가 죽은 id를 계속 폴링(discovery/상태변화 통지 채널 공백 = 마찰 #3와 동근). 맥 반영=`e073329`(`_mac-rc1.md ⑦`).

- **세션 8 (2026-07-03, 후반): A2A 스트리밍(SSE) Phase 2 완료 = 표준 A2A 서버로 스트리밍 지원.** "복붙 UX면 A2A 왜?"(복붙=트리거 릴레이, 코어는 이미 poll/get_task로 노출) 논의 -> 정찰은 이미 끝나 있었고(스펙 §9.1·§5.3 인용, `SubscribeToTask` 명명, 카드 streaming 플래그 존재) `§65 "SSE 후속" 유예를 interop·호기심 근거로 재개. 정본 [스트리밍 설계](docs/design/v2-a2a-streaming_2026-07-03.md). T1 이벤트 버스(store 계층 broadcast, /a2a·MCP 두 경로 자동 커버, `785fb25`) / T2 스트리밍 serde 타입+매핑(`25619c4`) / T3 `SendStreamingMessage` SSE(subscribe-before-create, `9ed6380`) / T4 `SubscribeToTask` 재구독(`ea3e855`) / T5 카드 `streaming:true`(`2bc5437`) / T6 **라이브 데모 성공(복붙 0)**: boss SSE 개방 -> 워커 MCP claim/complete -> 같은 버스로 submitted->working->artifact->completed(final) 실시간 스트림 후 종료. 검증 기본 218 / 풀피처 279 lib pass. **스코프 경계**: 워커 방향 push는 미구현(브로커 폴링 유지), push_notifications(webhook)·discovery는 후속(YAGNI).

- **세션 8 (2026-07-03, 후반2): 크로스머신 SSE 스트리밍 스모크 성공 + A2A 자율 워커 데몬(a·b) 완료.** 크로스머신 스모크: 맥=원격 dispatcher가 SendStreamingMessage를 LAN 너머 SSE로 개방 -> Windows worker claim/complete -> 맥 SSE에 4프레임 실시간 도착(SSE-over-LAN 실증). "복붙 아직 맞지?" 논의 = SSE가 데이터/완료통지는 제거, 남은 트리거 릴레이는 **워커 auto-poll로 제거** -> 자율 워커 데몬 착수. 정본 [워커 데몬](docs/design/v2-a2a-worker-daemon_2026-07-03.md). W1 프로덕션 MCP HTTP 클라(`ad5ca38`) / W2+W3 `tunaround work` 서브커맨드+자율 루프(poll->claim->Runner.run->complete, `60364d8`) / W4 **로컬 데모(사람 트리거 0)**: dispatcher SSE + `work --once --runner claude`가 자율 발견·claude 실행·완료, SSE에 claude 실답변 artifact 실시간. **(b) 이기종**: `--runner codex`로 **Codex가 워커**(GetTask=completed+codex 답변) = **(a)=(b), --runner만 교체=파트너 교체**(`a94809b`). Ollama-http(`--runner http`)는 코드완성이나 로컬 GPU OOM으로 라이브 미검증(후속). 검증 기본 218 / 풀피처+worker 286 lib pass. 스코프: opt-in 데몬, read-only 기본, dispatcher 사람 목표발행(semi-a2a HITL).

- **세션 8 (2026-07-03, 후반3): 워커 후속(fail전이·프로젝트라우팅·Ollama) + A2A interop 방향 확정 + outbound 표준 A2A 위임 실증.** (1) Ollama-http 워커 라이브 성공(GPU 언로드 후, `8c9f6d6` reqwest::blocking을 std::thread로 = 3번째 파트너 로컬LLM). (2) **fail 전이**(`abc4f1e`): 러너 실패 시 completed 위장 대신 `fail_task`로 state=failed(dispatcher가 성패 구분). (3) **context_id 프로젝트 라우팅**(`bc70e29`): poll에 ctx= 노출 + `--context-map`으로 데몬 하나가 여러 프로젝트 배분. (4) **A2A interop 스모크**(독립 a2a-client로 우리 코어 외부검증): GetTask/envelope는 호환이나 Agent Card(인증게이트+구식스키마)·SendMessage(브로커 fromAgent/toAgent 필수)는 제3자 미호환 = "표준 A2A" 문구 정직화(`e922534`, "A2A 기반"으로). (5) **방향 결정**(동구님): inbound(제3자가 우리한테) 폐기(오픈소스라 레포 쓰면 됨), **outbound(우리가 표준으로 던짐)만 구축**. (6) **A2ARunner**(`6399443`, WA1+WA2): a2a-client로 외부 표준 A2A 에이전트에 위임 = 4번째 러너 `--runner a2a`. **WA3 outbound interop 성공**: 독립 표준 서버(radkit) 상대 왕복 실증(카드발견->SendMessage->task완료->artifact). 검증 기본 218 / +worker+a2a-out 304 lib pass. 정본 [outbound 러너](docs/design/v2-a2a-outbound-runner_2026-07-03.md) · 사용법 [a2a-usage](docs/reference/a2a-usage.md).

- **세션 8 (2026-07-03, 후반4): 제미나이+코덱스 리뷰 기반 리팩토링을 A2A 3자 도그푸딩으로 수행(브랜치 `refactor/reviews-2026-07-03`, 8/9 완료).** 리뷰 삼분류→계획([리팩토링 계획](docs/plans/v2-refactor-from-reviews_2026-07-03.md), 원문 docs/reviews/). **3자 분담**: Opus 통합자(R4·R1R2·R10 = Sonnet 서브) / **Codex 워커 A2A**(R6·R3) / **Mac 워커 A2A LAN**(R5) / tunaLlama→직접(R8). 실버그 4개 잡음(R1 MCP성공위장·R2 이중claim·R3 부모만kill·R5 orphan). **도그푸딩 findings**: (a) R10=워커 세션만료 404 발견·수정(`c58df41`), (b) 동시 워커는 워크트리 격리 필요, (c) 워커=헤드리스 데몬(fresh spawn)이 handoff·/clear 불요, (d) tunaLlama는 config 필요, (e) 방법론=GitHub Flow+PR CI가 semi-a2a에 적합, (f) 통합자가 브랜치 push를 git-watch로 auto-poll=사람 릴레이 0. **남음**: R7(Mac, 큼)·브랜치 머지·PR CI 도입·usecase 문서. 브랜치 310 pass. **진입점=[session8-refactor 핸드오프](docs/prompts/v2-handoff_2026-07-03_session8-refactor.md).**

- **세션 5: 시간성·유효성 마무리(step 5c·6) + codex pull 활성화(behavioral) + 외래어 병기 색인 + 임베딩 기본 qwen3 + 배포(cargo-dist)·온보딩(clap 서브커맨드·tunaround.toml 프로파일) + AGPL-3.0 + 맥-윈도우 핸드오프.** 전부 origin/main 푸시(= c89da05).
- **이전 세션 4: Stage 3a-3(front=core) + Stage 3d(원격 쓰기 권위) + 시간성·유효성 로드맵 step 2~8.**
  - **3a-3**: `--core <addr>` 단일 프로세스(REPL+in-process HTTP MCP 코어). **서버=전용 OS 스레드 block_on**(공유 rt spawn은 유휴 중 간헐 신뢰불가). 라이브 e2e.
  - **3d(옵션 B front=core 병합)**: `append_turn`(증분·DB id 권위) + `post_turn`/`get_roster` MCP + REPL core-sync(adopt+append, 클로버 차단). 라이브 e2e: 원격 post_turn→흡수→claude 인용.
  - **로드맵(외부 memory 프레임워크 리뷰 후, SQLite-light·graph DB 비채택)**: step2 model_id 무효화키(실버그) · step3 retrieved 길이·세션 다양성 cap · step4 message_validity 테이블(스키마 v4) · step5 유효성 랭킹+/supersede·/reject · step5b 분기/세션 인지 랭킹 · step7 /explain 디버그 · step8 --reindex.
- **v1 + v2 검색/맥락 로드맵(step 2~8) + Stage 3a~3d + codex pull(behavioral) + 실코퍼스 회귀(step6) + 외래어 병기 + 임베딩 qwen3 + 배포/온보딩(clap·cargo-dist·프로파일) 완성.** 검증: **기본 184 lib+6 cli / `--features "semantic morphology mcp serve"` 198 lib+9 cli pass, clippy 클린(no-default 포함).** 스키마 **v5**(created_at).
- 현행 spec: [docs/design/tunaRound-v1-design_2026-06-29.md](docs/design/tunaRound-v1-design_2026-06-29.md). 진행: [docs/plans/index.md](docs/plans/index.md).
- **>>> 진입점 먼저 읽기 (각 줄은 해당 머신만 편집) <<<**
  - **WIN 최신**: [세션32](docs/prompts/v2-handoff_2026-07-19_session32.md) - **백로그 완주: #138 A·B 전량 + v0.6.0 릴리스·양 머신 배포 + 스윕(#115 메이저 5종·#147 좌석 수신함 Stage 1·C 실행분·다크 스크롤바) = #115/#138/#147 CLOSED + Kiwi 정리(#158~#160, serve=Kiwi 실매핑 판정).** 열린 PR 0, 열린 이슈=#151만. win mesh=rmcp 2.2 라이브. **다음=① v0.6.1 릴리스(patch 정책 확정, 도그푸딩 후 승인, mac 정렬=바이너리→훅 순) ② 오너 결정 2건(#151) ③ 트리거 대기(C-④a·#147 Stage2).** (이전: [세션31](docs/prompts/v2-handoff_2026-07-19_session31.md).)
  - **MAC 최신**: [session-longrun](docs/prompts/v2-handoff_2026-07-19_mac-session-longrun.md) - 맥 장기세션(07-10~19): **v2-44 presence-scanner·v2-46 codex-relay 마이그레이션** / **A2A 자율규약 게이트폐지**(동구님 직접승인, 메모리 [[a2a-boss-task-autonomy-protocol]]: 총괄발 task=무해·민감 전부 자율+보고, 파괴작업만 명시, 규약변경만 사람확인) / 릴리스 도그푸딩(v0.4.0→0.5.0 아티팩트) / codex 래퍼 `~/.local/bin` 배치(RC 편집 없이) / 유령poll 정리 / **start_discussion 설계토론 3라운드**(제품 본래 용도 라이브). **콜드스타트**: 재부팅했으면 `bash ~/.tunaround/restart-mac-mesh.sh`(v2-46: app-server+presence-scan+codex-relay). 자율수신=브로커 직접 poll_tasks Monitor(세션바운드, 새 세션 재구축). 교훈: 실행중 바이너리 원자교체(in-place cp 금지)·PID선별종료·빌드성공 확인후 배포·dashboard 피처 mac불요. (이전 [mac-autoarm-philosophy](docs/prompts/v2-handoff_2026-07-08_mac-autoarm-philosophy.md).)
- 이전 [session5](docs/prompts/v2-handoff_2026-07-02_session5.md). 맥↔윈도우 왕복은 [docs/reference/dev-mac-windows.md](docs/reference/dev-mac-windows.md). 협업 규약은 위 "맥↔윈도우 협업 규약" 섹션.
- **Cargo.toml `version="0.2.2"`**(세션10부터 정식 배포, 더 이상 rc 아님). cargo-dist 6타깃 + homebrew-tap 발행 중(cargo-release로 bump). 릴리스 교훈=[dev-mac-windows §6](docs/reference/dev-mac-windows.md). 정본 방향: [배포·온보딩](docs/design/v2-deploy-onboarding_2026-07-02.md) + [A2A](docs/design/v2-A2A-core-backend_2026-06-30.md) + [시간성·유효성](docs/design/v2-temporal-validity-direction_2026-07-01.md).
- **⚠ 서버 호스팅 교훈**: `--core`(=`core` 서브커맨드)는 메인이 동기 블로킹 REPL이라 서버를 **전용 스레드 block_on**으로 서빙(공유 rt spawn 신뢰불가). 라이브 e2e 타이밍 함정(Kiwi ~3초/FIFO 미flush/agent ~35초) → 준비 폴링 + 파이프 입력 + 넉넉한 타임아웃.
- **남은 항목**: 공개 릴리스=**`v0.1.0-rc.1` 먼저**(맥 도그푸딩 판정: 6타깃 CI 미검증이라 rc로 CI 검증 후 최종 태그. 상세 [release-readiness](docs/reference/release-readiness-v0.1.0_2026-07-02.md)) · 온보딩 Stage 4 doctor · abstraction/anchors 생성 파이프라인(보류=YAGNI) · **분산 크로스머신 스모크=claude leg 통과**(맥.184→윈도우.179 read_transcript 실증 2026-07-02), codex leg는 승인취약(#24135)→app-server(3e) 후속 · 홈랩 코어 호스팅(보류) · opencode 검색 배선.
- **맥 검증(2026-07-02, 완료)**: 맥 aarch64 빌드·테스트·`cargo install`·2에이전트 도그푸딩 전부 통과(크로스컴파일 이슈 없음). Kiwi 자산 404→lindera 폴백 정상.
- **완료된 이전 남은항목**(참고): step 5c·6·codex pull·codex bearer-env·잠재리뷰(bounded bus/snapshot log/Kiwi 주석) 전부 이번 세션에 처리됨.
- **검증/주의:** 임베딩=원격 Ollama(SSH `-p [사설포트]` 터널, dim 1024). 기본 모델 `qwen3-embedding:0.6b`(bge-m3보다 hybrid MRR 우위 측정), `TUNAROUND_EMBED_MODEL`로 교체. Redis 6379(3d/랭킹엔 불요).
- **Kiwi(정정 2026-07-02):** kiwi-rs 0.1.4는 **순수 Rust 빌드**(dep=regex만, build.rs·네이티브 링크 없음)라 **macOS/Win/Linux 모두 빌드됨**(kiwi cfg는 linux-aarch64만 제외). libkiwi(.dll/.dylib/.so)+모델은 **런타임에 bab2min/Kiwi 릴리스에서 다운로드**(캐시=OS cache dir 또는 `KIWI_LIBRARY_PATH`/`KIWI_MODEL_PATH`/`KIWI_RS_VERSION` env). 과거 "libkiwi 404"는 빌드가 아니라 런타임 자산 다운로드 실패(버전/자산)로, `scripts/install-kiwi-windows.sh`(Windows 전용)가 캐시를 pre-seed해 우회(맥/리눅스 전용 스크립트는 없음 - 자동다운로드 또는 lindera 폴백). 실패해도 **lindera 자동 폴백**이라 빌드·실행 안 죽음. **맥(aarch64) 실측 2026-07-02: libkiwi 0.23.1 dylib 로드 실패 + kiwi_mac_arm64_v0.23.2.tgz 자산 404 -> lindera 폴백으로 정상 동작.** bab2min/Kiwi v0.22.2에 맥 자산 존재(`kiwi_mac_arm64`/`kiwi_mac_x86_64`).

## 무엇을 만드나 (요약)

터미널에서 **사용자가 운전하는 역할 부여 2-에이전트(Claude Code·Codex) 착수 전 설계 토론** 도구. 같은 레포 위에서 사람 주도로 토론하고, 결론을 **결과 문서로 자동 기록**해 구현으로 넘긴다.

**핵심 결정(brainstorming 2026-06-29):** 사람 주도 대화형 / 공유 컨텍스트 = 같은 레포+공유 문서(컨텍스트팩 없음) / 읽기 전용 화자 + 사람이 쓰기 지목 / 순차-인지 턴 / 자리마다 역할 주입 / v1=2자리 고정 / consensus carry-forward(종료는 사람) / 스택 Rust+tokio.

**레이어(출처):** 에이전트 러너(tunaFlow `claude.rs`/`codex.rs` 포팅) + 토론 오케스트레이터(tunapi `core/roundtable/` 청사진 -> Rust 재구현) + 전사·영속(파일/rusqlite, 트리-ready) + 프론트(thin REPL).

**v1 비목표 -> v2:** Redis 멀티세션 = git-tree 다중 브랜치 / N>2 좌석 로스터(로컬LLM·opencode) / 리치 TUI(ratatui)·웹 / 협업 코딩.

## 출처 레포 (포팅 시 읽기)

- **tunapi**(전전신, Python): `~/privateProject/tunapi/src/tunapi/core/roundtable/` - 토론 오케스트레이터 청사진(`orchestrator.py`/`prompt.py`/`rt_participant.py`/`session.py`). 역할·순차-인지·follow-up·consensus.
- **tunaFlow**(Rust): `~/privateProject/tunaFlow/src-tauri/src/agents/{claude,codex}.rs` - CLI 러너(`stream_run`) + hardening.
- **tunaSalon**(Rust, v2용): `src/session_bus.rs`(Redis), `src/chat.rs`의 `render_chat`(ratatui), `src/flow.rs`(FlowMeter, 선택).

## 다음 세션 첫 행동

1. **[docs/prompts/v2-handoff_2026-07-03_session6.md](docs/prompts/v2-handoff_2026-07-03_session6.md) 먼저 읽기** + `context-notes.md`(상단 세션6) + `checklist.md`("semi-a2a 파트너 위임 Phase 1" 섹션) + 설계 정본 [파트너 위임](docs/design/v2-a2a-partner-delegation_2026-07-02.md). `cargo test`(기본 209) + `cargo test --features "semantic morphology mcp serve"`(262)로 상태 확인(**cargo는 Bash 툴로**).
2. **다음 세션 우선순위 = Task 5 라이브 도그푸딩 재개**(핸드오프 §3~④): 윈도우 코어 재기동(`target\debug\tunaround.exe serve 0.0.0.0:8770 --token [REDACTED_TOKEN] --db <임의 temp>`, PowerShell background) -> 맥 worker(mac-claude, `docs/prompts/a2a-dogfood-mac-worker_2026-07-03.md`) 붙었나 확인 -> `/a2a` SendMessage로 test task 던지고 -> GetTask로 **state=completed+artifact** 검증 = **왕복 1회 = Task 5 성공.** 이후 Phase 2(이기종 파트너 Codex-on-Ollama·A2A interop 갭·SSE). 릴리스(v0.1.0)·IP 히스토리 filter-repo 퍼지는 **배포 비우선**(핸드오프 §6).
   - **A2A 성숙도(정직)**: 현재=공유맥락(데이터평면)+**사람 오케스트레이션(HITL)** = **semi-a2a**(자율수준이 "semi"=HITL, A2A 통신은 진짜 성립). 스펙트럼: 수동relay < semi-a2a < full-auto(AutoLoop=Stage4 미구현, 의도적 보류). 크로스머신 앱-투-앱 위임 설계=docs/design/v2-a2a-partner-delegation_2026-07-02.md.
3. 작업 추적 `checklist.md`·`context-notes.md`(규율 #7). 위임 Sonnet + Opus 리뷰. 굵직한 결정 재론 금지. 서브에이전트 진행 중 파일 레이스 주의. 배포 전 도그푸딩.
